\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{setcommand}[2026/04/15 Custom Commands]

\providecommand{\quoteI}[1]{``#1''} % "" quotes
\providecommand{\quotei}[1]{`#1'} % '' quotes

\providecommand{\lfi}{ﬁ} % manual ligatures
\providecommand{\ff}{ﬀ}
\providecommand{\ffi}{ﬃ}
\providecommand{\fl}{ﬂ}
\providecommand{\ffl}{ﬄ}

\def\quade{\hspace{1.5em}} % big h space
\def\qua{\hspace{0.75em}}  % small h space
\DeclareMathOperator{\cosec}{cosec}

\newcommand{\textui}[1]{\begingroup\itshape\addfontfeature{FakeSlant=-0.2}#1\endgroup} % fake upright italic

\newcommand{\shrug}[1][]{
    \begin{tikzpicture}[baseline,x=0.8\ht\strutbox,y=0.8\ht\strutbox,line width=0.125ex,#1]
        \def\arm{(-2.5,0.95) to (-2,0.95) (-1.9,1) to (-1.5,0) (-1.35,0) to (-0.8,0)};
        \draw \arm;
        \draw[xscale=-1] \arm;
        \def\headpart{(0.6,0) arc[start angle=-40, end angle=40,x radius=0.6,y radius=0.8]};
        \draw \headpart;
        \draw[xscale=-1] \headpart;
        \def\eye{(-0.075,0.15) .. controls (0.02,0) .. (0.075,-0.15)};
        \draw[shift={(-0.3,0.8)}] \eye;
        \draw[shift={(0,0.85)}] \eye;
        % draw mouth
        \draw (-0.1,0.2) to [out=15,in=-100] (0.4,0.95);
    \end{tikzpicture}
} % "\_(''J)_/" from TeX Stack Exchange


% -------- SMALL CAPS --------
% \newcommand\fauxsc[1]{\fauxschelper#1 \relax\relax}
% \def\fauxschelper#1 #2\relax{%
%     \fauxschelphelp#1\relax\relax%
%     \if\relax#2\relax\else\ \fauxschelper#2\relax\fi%
% }
% \def\Hscale{.83}\def\Vscale{.72}\def\Cscale{1.00}
% \def\fauxschelphelp#1#2\relax{%
%     \ifnum`#1=\lccode`#1\relax\scalebox{\Hscale}[\Vscale]{\char\uccode`#1}\else%
%     \scalebox{\Cscale}[1]{#1}\fi%
%     \ifx\relax#2\relax\else\fauxschelphelp#2\relax\fi}


% -------- Old Style Num --------
% Use \oldstynum for counter
% Use \oldnumber for manual input
\def\oldstynum#1{\expandafter\oldnumber\csname c@#1\endcsname}
\def\oldnumber#1{%
    \olddigits{\number#1}%
}
\def\olddigits#1{\expandafter\oldigits #1@ }
\def\oldigits#1{%
    \ifx @#1% olen terminate
    \else
        \ifx0#1\scalebox{0.95}[0.85]0\else%
            \ifx1#1\scalebox{0.95}[0.85]1\else%
                \ifx2#1\scalebox{0.95}[0.85]2\else%
                    \ifx3#1\raisebox{-0.4ex}{3}\else%
                        \ifx4#1\raisebox{-0.4ex}{4}\else%
                            \ifx5#1\raisebox{-0.4ex}{5}\else%
                                \ifx6#1{6}\else%
                                    \ifx7#1\raisebox{-0.4ex}{7}\else%
                                        \ifx8#1{8}\else%
                                            \ifx9#1\raisebox{-0.4ex}{9}\else%
                                                #1\fi\fi\fi\fi\fi\fi\fi\fi\fi\fi
        \expandafter\oldigits
    \fi
}


% -------- Time --------
\newcommand{\mytime}{%
    \count255=\time
    \divide\count255 by 60
    \the\count255.% Hour
    \count255=\time
    \count254=\count255
    \divide\count254 by 60
    \multiply\count254 by 60
    \advance\count255 by -\count254
    \ifnum\count255<10 0\fi\the\count255% Minute (with leading zero)
}

% \RequirePackage{pgfcalendar} % day utilities

%% Day note
% \newcommand{\daynote}[2][]{%
%     \sidenote[\hspace*{-0.6em}]{%
%         \oldnumber{#2} \\ \dayof{#2} #1}%
%     \vadjust{\vspace{-2\notebooklineskip}}\par%
% }

% \def\dayof#1{\splitdate#1\relax}
% \def\splitdate#1/#2/#3\relax{
%     \newcount\dow%
%     \pgfcalendardatetojulian{#3-#2-#1}{\dow}%
%     \pgfcalendarjuliantoweekday{\dow}{\dow}%
%     \ifcase\dow  Monday\or  Tuesday\or  Wednesday\or  Thursday\or  Friday\or  Saturday\else  Sunday\fi
% }

% \newcommand{\pagetarget}[2]{%
%     \phantomsection%
%     \label{#1}%
%     \hypertarget{#1}{#2}%
% }

% \newcommand{\monthnote}[1]{%
%     \sidenote[\hspace*{-1.3em}]{%
%         \nolinebreak%
%         \raisebox{0.5ex}{\MiniPostIt[yellow]{\raisebox{-0.5ex}{\bfseries\ \pagetarget{#1}{#1}}}}}%
%     \vadjust{\vskip-1\baselineskip}%
% }

% \newcommand{\xdaynote}[1]{\phantom{}\hfill --- {\itshape #1}}


% -------- MATH --------
% Colored aligned box (need mathtools pkg)
\newcommand*\Acolorboxed[2][red]{%
    \let\bgroup{\romannumeral-`}%
    \@Acolorboxed{#1}#2&&\ENDDNE
}
\def\@Acolorboxed#1#2&#3&#4\ENDDNE{%
    \ifnum0=`{}\fi
    \setbox\z@\hbox{$\displaystyle#2{}\m@th$\kern\fboxsep \kern\fboxrule}%
    \edef\@tempa{\kern\wd\z@ & \kern-\the\wd\z@ \fboxsep\the\fboxsep \fboxrule\the\fboxrule}%
    \@tempa
    \fcolorbox{#1}{white}{\m@th$\displaystyle#2#3$}%
}


% -------- FUN --------
% Emoji counter (bxcoloremoji pkg)
% \def\emojinum#1{\expandafter\emojinumber\csname c@#1\endcsname}
% \def\emojinumber#1{%
%     \emojidigits{\number#1}%
% }
% \def\emojidigits#1{\expandafter\emdigits #1@ }
% \def\emdigits#1{%
%     \ifx @#1% emen terminate
%     \else
%     \ifx0#1{\coloremoji{0️⃣}}\else\ifx1#1{\coloremoji{1️⃣}}\else\ifx2#1{\coloremoji{2️⃣}}\else\ifx3#1{\coloremoji{3️⃣}}\else\ifx4#1{\coloremoji{4️⃣}}\else\ifx5#1{\coloremoji{5️⃣}}\else\ifx6#1{\coloremoji{6️⃣}}\else\ifx7#1{\coloremoji{7️⃣}}\else\ifx8#1{\coloremoji{8️⃣}}\else\ifx9#1{\coloremoji{9️⃣}}\else#1\fi\fi\fi\fi\fi\fi\fi\fi\fi\fi
%     \expandafter\emdigits
%     \fi
% }
% \AddEnumerateCounter{\emojinum}{\emojinum}{M}
% \newlist{emojienum}{enumerate}{3}
% \setlist[emojienum]{label=\emojinum*}
% \renewcommand{\thepage}{\emojinum{page}}

% \RequirePackage{JeuxCartes} % card games

% \RequirePackage{xskak} % chess

% \RequirePackage{musixtex} % music sheets
% \RequirePackage{musicography} % music figures

% \RequirePackage{rubikcube, rubikrotation} % rubik's cube
% \RequirePackage{rubikpatterns, rubiktwocube}

% \RequirePackage{qrcode} % qrcodetikz is better - 2026 version required

% \RequirePackage[l3]{csvsimple} % csv data with LaTeX3 version

% \RequirePackage{mercatormap} % geographical map tiles
%% terminal: xelatex --shell-escape main.tex (Python3 required)
%% preamble: \mrcactivatescript
%% api key : \mrcsetapikey{thunderforest}{30c5c964e8d64c7490fc86743a8ef537}

% \RequirePackage{annotate-equations} % annotated equations

% \RequirePackage{pgf-pie} % pie charts

% \RequirePackage{pgf-umlcd} % plant UML parser

% \RequirePackage{verse} % typesetting verse

% \RequirePackage{minted,termsim} % terminal simulation (slow compile time)
% \termset{
%   theme=light, % dark / light / white
%   os=win10     % win10 / mac / ubuntu
% }

% \RequirePackage{postit} % Note papers
%% PostItNote env. / \MiniPostIt[color]{text}
%% Render = / tikz / tikzv2
%% Pin = / Paperclip / Scotch / None
%% PinColor, Color, Corner, Width, Height, Rotate
%% ExtraRightMargin=l, Title={text}, FontTitle={font}
%% StorePostIt=name to mark tikz node

% \RequirePackage{tikz-mirror-lens} % optics

% \RequirePackage{hanzibox} % Chinese box

% \RequirePackage{weiqi} % go game

% \RequirePackage{pxpic} % pixelated art

% \RequirePackage{tikzbricks} % toy bricks
% \RequirePackage{tikzbrickfigurines} % mini figures

% \RequirePackage{algorithm2e} % floating algorithm figures

% \RequirePackage{hologo} % logos of LaTeX and friends

% \RequirePackage{longdivision} % \intdivision \longdivision[]{22}{7}
% \longdivisionkeys{separators in work=false} % stage=0, max extra digits=2

% \RequirePackage{xsim} % exercise solution

% \RequirePackage{smart-eqn} % math style configuration

% \RequirePackage{planets} % illustrate celestial mechanics

% \RequirePackage{newspaper} % newspaper page style
% \RequirePackage{lettrine} % drop caps for newspaper
%% \lettrine[lhang=0.3, lines=2, findent=0.2pt, nindent=0.1pt]{T}{he}

% \RequirePackage{keystroke} % keyboard visuals
% \RequirePackage{menu} % menu instruction

% \RequirePackage{example} % content and code display

% \RequirePackage{animate} % make animations
% \RequirePackage{media4svg} % embed videos (require dvisvgm)

% \RequirePackage{graphviz} % dot diagrams

\endinput

setcommand.sty, version 1.1

This package provides custom control sequences and other specific packages.

See https://github.com/goldhand-c